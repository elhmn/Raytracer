#include "libft.h"
#include "transform.h"

t_pos		transform(t_base a, t_base b, t_pos pos)
{
	t_pos	tmp;

	(void)a;
	(void)b;
	(void)pos;
	(void)tmp;
	return (tmp);
}

t_pos		transform_rev(t_base a, t_base b, t_pos pos)
{
	t_pos	tmp;

	(void)a;
	(void)b;
	(void)pos;
	(void)tmp;
	return (tmp);
}
